import type { ChannelOnboardingAdapter } from "../onboarding-types.js";

/** Stub - ${channel} onboarding not supported in hive-gateway. */
export const signalOnboardingAdapter: ChannelOnboardingAdapter = {};
