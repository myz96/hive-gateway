/** Stub - iMessage not supported in hive-gateway. */
export function parseIMessageTarget(_target: string): string | undefined {
  return undefined;
}

export function normalizeIMessageHandle(handle: string): string {
  return handle;
}
